[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/apps/user-service/apps/user-service/src/main"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
